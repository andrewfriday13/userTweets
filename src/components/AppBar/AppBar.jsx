const { default: Navigation } = require("components/Navigation/Navigation")


const AppBar = () =>{
    return(
        <header>
            <Navigation/>
        </header>
    )
}

export default AppBar